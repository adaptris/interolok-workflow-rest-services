openapi: 3.0.1
info:
  title: Interlok Workflow REST services
  description: A REST entry point into your Interlok workflows.
  version: "1.0"
servers:
  - url: 'http://localhost:8080'
paths:
  /workflow-services/MyInterlokInstance/JMS-JMS/eager-albattani:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      - text/xml
      - text/html
      parameters:
      - name: body
      in: body
      required: true
      schema:
      type: object
      properties:
      testapi:
      type: object
      properties:
      adapter:
      type: string
      example: MyInterlokInstance
      channel:
      type: string
      example: Channel-0000000
      id:
      type: string
      example: Workflow-000000
      testapiBody:
      type: object
      properties:
      type:
      type: object
      properties:
  /workflow-services/MyInterlokInstance/FS-JMS/suspicious-noyce:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      - text/xml
      - text/html
      parameters:
      - name: body
      in: body
      required: true
      schema:
      type: object
      properties:
      testapi:
      type: object
      properties:
      adapter:
      type: string
      example: MyInterlokInstance
      channel:
      type: string
      example: Channel-0000000
      id:
      type: string
      example: Workflow-000000
      testapiBody:
      type: object
      properties:
      type:
      type: object
      properties: